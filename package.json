{
  "name": "student-management-root",
  "private": true,
  "scripts": {
    "postinstall": "cd backend && npm install",
    "build": "cd backend && npx prisma generate",
    "start": "node backend/server.js"
  }
}
